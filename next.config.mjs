/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['randomuser.me', 'another-example.com'], // Replace with your image domains
      }
};

export default nextConfig;
